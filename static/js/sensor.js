/* script to dyanically update sensors */
function updateSensors_Live(interval = 2, sensor_tableid){
    var sensor_table = sensor_tableid;
    for (var sens = 0; sens < sensor_table.length; sens++){
        
    }
    setInterval(function(){

    }, interval*1000);
}